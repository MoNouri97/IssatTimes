import React from 'react';
import { Text, View } from 'react-native';

const TestScreen: React.FC = ({}) => {
	return (
		<View>
			<Text style={{ backgroundColor: 'red', width: '100%', height: 3 }}>
				...
			</Text>
		</View>
	);
};
export default TestScreen;
